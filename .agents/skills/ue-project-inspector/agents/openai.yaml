interface:
  display_name: "UE Project Inspector"
  short_description: "Inspect UE project identity and structure"
  default_prompt: "Use $ue-project-inspector to identify the current Unreal project and inspect only the requested project facts."
